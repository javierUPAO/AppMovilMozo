<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="170dp"
    android:layout_height="170dp"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    android:background="@android:color/transparent"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="#FFFF"
        android:padding="10dp"
        android:clickable="false"
        android:focusable="false">

        <!-- ICONO + ID CENTRADO -->
        <FrameLayout
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:clickable="false"
            android:focusable="false">

            <ImageView
                android:id="@+id/ivIconoMesa"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/ic_table_new"
                android:scaleType="fitCenter"
                android:clickable="false"
                />

            <TextView
                android:id="@+id/tvIdMesa"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="m1"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="#616161"
                android:clickable="false"
                android:focusable="false"/>
        </FrameLayout>

        <!-- Estado abajo del icono (ej. "OCUPADA") para evitar superposición -->
        <TextView
            android:id="@+id/tvEstado"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textStyle="bold"
            android:layout_marginTop="2dp"
            android:clickable="false"
            android:focusable="false"
            tools:text="OCUPADA"
            tools:textColor="#F5A623"/>

        <!-- TEXTO ABAJO (Capacidad) -->
        <TextView
            android:id="@+id/tvCapacidad"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:layout_marginTop="2dp"
            tools:text="3 Personas"
            android:clickable="false"
            android:focusable="false"
            />

        <!-- Alerta de Demora y Tiempo de Espera -->
        <LinearLayout
            android:id="@+id/llAlertaDemora"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="4dp"
            android:visibility="gone"
            tools:visibility="visible">

            <ImageView
                android:id="@+id/ivAlertaDemora"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:src="@drawable/ic_warning"
                android:layout_marginEnd="4dp"
                android:clickable="false"
                android:focusable="false" />

            <TextView
                android:id="@+id/tvTiempoEspera"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="#C0392B"
                tools:text="DEMORA: 12 min"
                android:clickable="false"
                android:focusable="false" />
        </LinearLayout>
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>